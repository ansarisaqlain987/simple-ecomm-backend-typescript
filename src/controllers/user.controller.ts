import { Controller, Request, Response } from '../types';

export const registerUser: Controller = (
  request: Request,
  response: Response,
) => {
  return response.send({
    data: null,
    errors: null,
  });
};

export const updateUser: Controller = (
  request: Request,
  response: Response,
) => {
  return response.send({
    data: null,
    errors: null,
  });
};

export const resetPassword: Controller = (
  request: Request,
  response: Response,
) => {
  return response.send({
    data: null,
    errors: null,
  });
};

export const login: Controller = (request: Request, response: Response) => {
  return response.send({
    data: null,
    errors: null,
  });
};

export const details: Controller = (request: Request, response: Response) => {
  return response.send({
    data: null,
    errors: null,
  });
};

export const userList: Controller = (request: Request, response: Response) => {
  return response.send({
    data: null,
    errors: null,
  });
};
