import { Controller, Request, Response } from '../types';

export const addProduct: Controller = (
  request: Request,
  response: Response,
) => {
  return response.send({
    data: null,
    errors: null,
  });
};

export const updateProduct: Controller = (
  request: Request,
  response: Response,
) => {
  return response.send({
    data: null,
    errors: null,
  });
};

export const deleteProduct: Controller = (
  request: Request,
  response: Response,
) => {
  return response.send({
    data: null,
    errors: null,
  });
};

export const productDetails: Controller = (
  request: Request,
  response: Response,
) => {
  return response.send({
    data: null,
    errors: null,
  });
};

export const productList: Controller = (
  request: Request,
  response: Response,
) => {
  return response.send({
    data: null,
    errors: null,
  });
};
